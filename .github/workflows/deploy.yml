name: Deploy to CPanel

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: |
          frontend/package-lock.json
          backend/package-lock.json
    
    - name: Install and build frontend
      run: |
        cd frontend
        npm ci
        npm run build
        echo "✅ Frontend build completed"
        ls -la dist/
        
    - name: Install and build backend
      run: |
        cd backend
        npm ci
        npm run build
        echo "✅ Backend build completed"
        ls -la dist/
        
    - name: Prepare unified deployment
      run: |
        mkdir -p unified-deploy
        
        # Copy frontend files to root
        cp -a frontend/dist/. unified-deploy/
        
        # Create api directory and copy backend files
        mkdir -p unified-deploy/api
        cp -r backend/dist/* unified-deploy/api/
        cp backend/package.json unified-deploy/api/
        cp backend/package-lock.json unified-deploy/api/
        
        # Create production .env file for API
        cat > unified-deploy/api/.env << EOF
        NODE_ENV=production
        PORT=3000
        FRONTEND_URL=https://marinemanagementsystem.com
        DATABASE_URL="mysql://username:password@localhost:3306/mms_db"
        SMTP_HOST=smtp.gmail.com
        SMTP_PORT=587
        SMTP_USER=your-email@gmail.com
        SMTP_PASS=your-app-password
        FROM_EMAIL=noreply@marinemanagementsystem.com
        CONTACT_EMAIL=info@marinemanagementsystem.com
        DEMO_EMAIL=demo@marinemanagementsystem.com
        JWT_SECRET=mms-super-secret-jwt-key-2024-production
        JWT_EXPIRE=30d
        RATE_LIMIT_WINDOW_MS=900000
        RATE_LIMIT_MAX_REQUESTS=100
        API_KEY=mms-api-key-production
        EOF
        
        echo "✅ Unified deployment ready"
        echo "📂 Directory structure:"
        echo "Frontend files:"
        ls -la unified-deploy/ | head -10
        echo "API files:"
        ls -la unified-deploy/api/ | head -10
        echo "Locale files:"
        find unified-deploy/locales -name "*.json" | head -5
        
    - name: Deploy everything to public_html
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.CPANEL_FTP_HOST }}
        username: ${{ secrets.CPANEL_FTP_USER }}
        password: ${{ secrets.CPANEL_FTP_PASSWORD }}
        local-dir: unified-deploy
        server-dir: public_html
        dangerous-clean-slate: true
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.DS_Store
        
    - name: Deployment Summary
      run: |
        echo "🎉 Unified deployment completed!"
        echo "📂 All files deployed to: public_html/"
        echo "├── index.html (Frontend)"
        echo "├── assets/ (Frontend CSS/JS)"
        echo "├── locales/ (Languages)"
        echo "├── *.png, *.jpg (Images)"
        echo "├── .htaccess (URL routing)"
        echo "└── api/ (Backend API)"
        echo "    ├── app.js"
        echo "    ├── .env"
        echo "    └── package.json"
        echo ""
        echo "🌐 Website URL: https://marinemanagementsystem.com"
        echo "🔗 API URL: https://marinemanagementsystem.com/api/health"
        echo ""
        echo "🔧 CPanel Node.js Settings:"
        echo "   - Application Root: public_html/api"
        echo "   - Application URL: marinemanagementsystem.com"
        echo "   - Startup File: app.js"
        echo "   - Port: 3000 (auto-detected)"
        echo ""
        echo "📝 Next Steps:"
        echo "   1. CPanel → Node.js → Restart App"
        echo "   2. Run NPM Install"
        echo "   3. Test: /api/health endpoint" 