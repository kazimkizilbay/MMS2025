# Server Configuration
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:5173

# Database Configuration
DATABASE_URL="mysql://username:password@localhost:3306/mms_db"

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/mms-database

# MongoDB Atlas (Online Database) - Replace with your Atlas connection string
MONGODB_ATLAS_URI=mongodb+srv://mms-user:YOUR_PASSWORD@mms-cluster.xxxxx.mongodb.net/mms_database?retryWrites=true&w=majority

# MySQL Configuration (for cPanel)
MYSQL_HOST=localhost
MYSQL_USER=marinema_superadmin
MYSQL_PASSWORD=kk197-xJ.
MYSQL_DATABASE=marinema_admin

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=noreply@yourdomain.com
CONTACT_EMAIL=info@yourdomain.com
DEMO_EMAIL=demo@yourdomain.com

# JWT Configuration (for future use)
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRE=30d

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# API Keys (if needed)
API_KEY=your-api-key-here 